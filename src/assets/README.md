# Assets

Place your artwork images in this folder.

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`

Reference them in `src/data/gallery.ts` using:

```ts
placeholder: "url('/src/assets/your-artwork.jpg')",
```

The `favicon.svg` here is the browser tab icon — replace with your final mark.
